# Five Automated Workflows

1. Customer request → AI classification → priority → action/review → activity log.
2. High-priority request → decision rule → human review → approve/reject → notification.
3. Invoice → extraction → classification → amount rule → approval task → audit.
4. Meeting notes → AI analysis → action items → tasks → notification.
5. Scheduled operational report → collect data → analyze → generate report → record result.
